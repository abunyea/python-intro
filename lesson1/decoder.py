message = input("Enter a message: ")

decodedMessage = message.replace('*', 'a')
# Fill in the rest
# Look at encoder.py to see what characters to replace with what

print(f"Decoded message: {decodedMessage}")
